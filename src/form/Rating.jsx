import React from 'react';
import { Grid, Typography, Rating } from '@mui/material';

const RatingField = ({
    name,
    label,
    data,
    error,
    onChange,
    required = true,
    ...rest
}) => {
    return (
        
    );
};

export default RatingField;
