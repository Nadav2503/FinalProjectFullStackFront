import React from "react";

const CheckboxField = ({
    name,
    data,
    label,
    required = false,
    error,
    onChange,
    ...rest
}) => {
    return (
        <Grid >
            <Typography >

            </Typography>

        </Grid>
    );
};

export default CheckboxField;
