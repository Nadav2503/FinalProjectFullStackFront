import React from "react";
import { TextField, Grid } from "@mui/material";


const Input = ({

}) => {


    return (
        <Grid >
            <TextField
            />
        </Grid>
    );
};

export default Input;