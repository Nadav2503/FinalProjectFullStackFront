import React from 'react';



const ReviewForm = ({
    onSubmit,
    onInputChange,
    errors = {},
    data = {},
    title,
    submitLabel,
    validateForm
}) => {
    return (
        <></>
    );
};

export default ReviewForm;
