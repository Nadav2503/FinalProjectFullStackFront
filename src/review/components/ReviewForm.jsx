import React from 'react';
import Form from '../../form/Form';


const ReviewForm = ({
    onSubmit,
    onInputChange,
    errors = {},
    data = {},
    title,
    submitLabel,
    validateForm
}) => {
    return (
        <Form

        >


        </Form>
    );
};

export default ReviewForm;
