import React from "react";
import Form from "../../form/Form";


const LoginForm = ({
    onSubmit,
    onInputChange,
    errors = {},
    data = {},
    title,
    submitLabel,
    validateForm
}) => {
    return (
        <Form
        >
        </Form>
    );
};

export default LoginForm;
